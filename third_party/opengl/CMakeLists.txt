project(opengl)
include(ExternalProject)

message("Project: ${PROJECT_NAME}")

find_package(PkgConfig REQUIRED)
find_package(OpenGL REQUIRED)
pkg_check_modules(GLEW REQUIRED glew)

message("OPENGL_INCLUDE_DIRS = ${OPENGL_INCLUDE_DIR}")
set(OPENGL_INCLUDE_DIR "${OPENGL_INCLUDE_DIR}" PARENT_SCOPE)
set(GLEW_INCLUDE_DIRS "${GLEW_INCLUDE_DIRS}" PARENT_SCOPE)

# set(OPENGL_LIBRARY_DIR "${OPENGL_LIBRARY_DIR}" PARENT_SCOPE)
set(GLEW_LIBRARY_DIR "${GLEW_LIBRARY_DIR}" PARENT_SCOPE)

set(OPENGL_LIBRARIES "${OPENGL_LIBRARIES}" PARENT_SCOPE)
set(GLEW_LIBRARIES "${GLEW_LIBRARIES}" PARENT_SCOPE)

set(ROCKIT_DEFINITIONS "${ROCKIT_DEFINITIONS} ROCKIT_RENDERER_OPENGL=1" PARENT_SCOPE)